spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/sms?useSSL=false
    username: postgres
    password: ld2erw71904
    driver-class-name: org.postgresql.Driver
    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mvc:
    hiddenmethod:
      filter:
        enabled: true
        
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
